<!-- GSAP + ScrollTrigger + Slick Carousel -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

<!-- Instructions -->

<!-- How to Implement -->
1. Create a container and add class `card-container` and make it sticky.
2. Inside the container, create a container and add class `card-wrapper`.
3. Inside the `card-wrapper`, add a spacer and add class `card-overlay`.
4. Add a sibling container and add your content (title, description, button, subtitle, etc).
5. Add the following JS and CSS in code snippet or child theme.
6. Make sure you have a full height section 100vh.
7. You can add multiple card blocks by duplicating them and add z-index to each of them. The last one will have a higher z-index than the first one.
